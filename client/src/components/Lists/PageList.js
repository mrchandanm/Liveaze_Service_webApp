import { Avatar, List, Space } from 'antd'
import React from 'react'
import { LikeOutlined, MessageOutlined, StarOutlined } from '@ant-design/icons';

const PageList = ({services}) => {

   

  return (
     <div>
    </div>
  )
}

export default PageList
